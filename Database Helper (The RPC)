CREATE OR REPLACE FUNCTION decrement_inventory(row_id UUID, amount INT)
RETURNS void AS $$
BEGIN
  UPDATE inventory
  SET current_stock = current_stock - amount
  WHERE id = row_id;
END;
$$ LANGUAGE plpgsql;
